# Claude Code: personal/local-only files only (never shared with the team).
# CLAUDE.md and .claude/agents|commands|settings.json are COMMITTED directly on
# main (this repo is main-only, no dev branch -- see CLAUDE.md's note on this).
CLAUDE.local.md
.claude/settings.local.json

# Other AI tool configs -- not used in this repo; block if ever introduced accidentally
AGENT-HANDOFF.md
AGENTS.md
.cursor/
.mcp.json
.cursorrules
.aider*
.windsurf/
.copilot/
.continuedev/
.codeium/
